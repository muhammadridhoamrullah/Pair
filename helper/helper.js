const helper = {

    addCmToHeight: (height) => {
    
        return `${height} cm`
    },

    addKgToWeight: (weight) => {
        return `${weight} kg`
    }

}

module.exports = helper;